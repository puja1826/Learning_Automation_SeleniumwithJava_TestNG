package BasicsSelenium;

public class WaitOverview {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		// Wait is the Sync between the script(Selenium) and the application running.
		// Static Wait(Thread.sleep)->pause of script
		// Dynamic Wait

//        In dynamic wait if the wait time is 6 Seconds and the element is found in 
//        2 seconds the remaining wait of 4 seconds is ignored 		
//		Dynmaic wait -Implicitly wait(Global wait)
////		              Explicit wait - a. Webdriver Wait(Class)
//		                                    b.Fluent Wait(Class
//		Webdriver wait is the child class of the Fluent wait

//		WebDriverWait(c)->Extends FluentWait->Implements->Wait(I) --until();

//		Wait(I)--until();
//		FluentWait--class--@override until() {} +other methods +TimeOutException()
      //WebDriver Wait--Class ---Has only one method timeOutException()
	}

}
